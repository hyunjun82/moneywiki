---
name: article-writing
description: "머니위키 글 작성 SKILL. 키워드를 받으면 Q1-Q4 사고 → 검색 리서치 → 타이틀 → H2 구조 → 컴포넌트 선택 → TSX 페이지 생성. 글 작성, 수정, 리라이트 시 사용. 사용법: /article-writing [신규|리라이트] [메인 키워드]"
argument-hint: "[신규|리라이트] [메인 키워드]"
allowed-tools: Read, Write, Edit, Grep, Glob, Bash(node scripts/*), WebFetch, WebSearch
---

# 머니위키 글 작성 SKILL

## 상세 가이드 (필요 시 참조)
- 컴포넌트 선택·Props: [component-guide.md](component-guide.md)
- 문체·접속사·글쓰기 규칙: [writing-rules.md](writing-rules.md)

---

## 모드 분기

### 신규 모드 (`/article-writing 신규 [키워드]`)
1. 메인 키워드 분석 → 허브 타이틀 생성
2. 롱테일 스포크 10~15개 키워드 자동 도출 (사용자 확인 후 진행)
3. 허브 page.tsx + 각 스포크 page.tsx 순차 작성
4. 각 파일 작성 시 PreToolUse/PostToolUse 훅이 자동 검증

### 리라이트 모드 (`/article-writing 리라이트 [키워드]`)
1. 기존 page.tsx 읽기
2. Q1-Q4 재사고 (기존 글의 문제점 파악)
3. 기존 구조 분석 → 개선점 도출 → 리라이트
4. 기존 URL 유지, 내용만 업그레이드

---

## ★ 필수 사고 (Step 0 — 모든 글의 첫 단계)

글 작성 전에 아래 4개 질문에 답한다. 이 답이 타이틀·H2·컴포넌트를 결정한다.
답은 파일 상단 주석 블록으로 남긴다.

Q1. 이 키워드를 검색하는 사람은 지금 어떤 상황인가?
Q2. 이 사람이 이 글을 읽고 나서 할 수 있어야 하는 행동은?
Q3. 이 행동을 하려면 반드시 알아야 하는 정보는?
Q4. 이 정보를 가장 잘 전달하는 형태는? (16개 컴포넌트 중 선택)

### Q→구조 연결 규칙
- Q2 답 → 타이틀 line2 + H2 순서 결정
- Q3 답 → H2 개수·깊이 결정
- Q4 답 → 섹션별 컴포넌트 결정
- Q1 답 → 도입부 톤·문장 결정

---

## ★ 토스피드 5원칙

1. **독자 중심** — 20대~80대 누구나 이해. 전문용어 뒤 괄호 설명 필수
2. **핵심만** — 불필요한 문장 없는 글. filler 전면 금지
3. **타이틀 + H2 = 검색의도 100%** — 소제목만 훑어봐도 "다 있네"
4. **소제목마다 시각화** — 텍스트만 있는 섹션 금지. 컴포넌트 1개씩
5. **문제해결 100%** — 이 글 하나로 행동까지 완료. "전문가에게" 회피 금지

---

## 0. 타이틀 생성 규칙

### 공식
[핵심 상황/키워드], [독자 질문]? [해결 범위] + [끝맺음 패턴]

### 좋은 예시
- "퇴직금, 10년 근무하면 얼마 받을까? 계산법부터 세금까지"
- "실업급여 받을 수 있을까? 자격 조건과 금액 계산법"
- "전세 계약 전에 확인해야 할 것들, 사기 예방 체크리스트"

### 끝맺음 패턴 (반드시 다양하게)
~까지 / ~계산법 / ~체크리스트 / ~방법 / ~기준 / ~조건
같은 카테고리 글끼리 동일 끝맺음 패턴 3회 이상 금지.

### 금지
- "총정리", "완벽정리", "A to Z", "한눈에"
- "알아보겠습니다", "살펴보겠습니다"
- 파이프형 (|) 타이틀
- 40자 초과

---

## 0-1. 소제목(H2) 설정 규칙

### ★ 최상위 원칙: 타이틀이 묻는 질문에 H2-1이 바로 답한다

**H2 = 타이틀 line2 키워드를 섹션별로 펼치되, 검색 의도에 맞는 순서로 배치**

### 검색 의도 분류

| 의도 | 타이틀 신호 | H2-1 (결론 먼저) | 이후 흐름 |
|------|-----------|-----------------|----------|
| **얼마형** | ~얼마, ~세금, ~금액 | Calculator (답 먼저) | GreenBox → Steps → Checklist |
| **가능형** | ~할 수 있나, ~되나요 | GreenBox(결론) + EligibilityChecker | Calculator → Steps |
| **방법형** | ~방법, ~어떻게, ~절차 | Steps (절차 먼저) | DocTable → Calculator |
| **비교형** | ~vs, ~차이, ~어떤 게 | CompareTable 또는 DiagnoseCard | Calculator → Steps |
| **개념형** | ~란, ~이란, ~무엇 | GreenBox(정의) + BorderBox | EligibilityChecker → Calculator |

**주의**: 위 테이블은 출발점. Q1-Q4 답에 따라 달라져야 한다.

### 톤 배분
- 질문형 2개 + 서술형 2개 + 행동형 1개
- 같은 톤 3연속 금지
- 타이틀 line2 키워드가 H2에 최소 3개 반영

### 금지
- H2에 숫자 번호 금지 (## 1. 제목 X)
- H2에 대시(ㅡ, —) 금지

---

## 1. 파일 구조

```
src/app/w/{slug}/
├── page.tsx      ← "use client" 인터랙티브 글
└── layout.tsx    ← metadata export (title, og, canonical)
```

page.tsx: `"use client"` → import 컴포넌트 → 데이터 상수 → Page 함수
layout.tsx: Metadata export (title, description, og, canonical)

---

## 7. 글 작성 절차

### Step 0: 필수 사고 (Q1-Q4) — 생략 금지
1. 키워드를 받으면 **신규/리라이트** 먼저 확인
2. Q1~Q4에 답한다
3. 답을 파일 상단 주석 블록으로 기록

### Step 0-1: 계산기/양식 페이지 체크 (리라이트 전 필수!)
1. `content/wiki/{slug}.md`의 `schemaType` 확인
2. `schemaType: calculator` 또는 `schemaType: form` → **리라이트 금지**

### ★ Step 0.5: 검색 리서치 (신규)
**목적: 실제 검색 데이터로 Q1-Q4 답을 보강하고, H2 구조를 최적화**

1. **WebSearch** — 키워드로 구글/네이버 검색
2. **상위 3개 결과 WebFetch** — 상위 노출 글의 H2 구조·커버 범위 파악
3. **PAA 수집** — "사람들이 궁금해하는 것" (People Also Ask) 추출
4. **검색 의도 확정** — 정보형/행동형/비교형 중 어떤 것인지
5. **H2 구조에 PAA 반영** — 독자가 실제 궁금한 질문을 H2로 배치

**리서치 결과 활용:**
- PAA 질문 → H2 소제목 또는 FAQ에 반영
- 상위 글에서 다루는 내용 → 반드시 커버 (누락하면 노출 불가)
- 상위 글에서 빠진 내용 → 우리 글의 차별점 (Information Gain)

### Step 1: 타이틀 생성 (Q2 + 리서치 기반)
1. Q2 답(독자가 할 행동) → 타이틀 line2 결정
2. `node scripts/suggest-structure.js "타이틀"` 실행 → 보조 참고

### Step 2: H2 구조 설계 (Q2→Q3 + PAA 기반)
1. Q2 답 → H2 순서 결정
2. Q3 답 + PAA → H2 개수·깊이 결정
3. 소제목 4~6개 — H2-1이 타이틀 질문에 바로 답하는 구조

### Step 3: 컴포넌트 매핑 (Q4 기반)
→ 상세: [component-guide.md](component-guide.md)

### Step 4: 소스 확보
1. 공식 출처에서 데이터 확보 (WebFetch 우선, WebSearch fallback)
2. 소스에 없는 정보로 팩트 채우기 **절대 금지**
3. 법령: law.go.kr / 세금: nts.go.kr / 복지: bokjiro.go.kr 등

### Step 5: 글 작성
→ 문체·접속사: [writing-rules.md](writing-rules.md)
1. page.tsx + layout.tsx 생성
2. 데이터 상수 → 컴포넌트 import → 페이지 조합
3. 구어체, 자연스러운 접속사, 논리적 흐름 준수

### Step 6: 검증
1. `npm run build` — 빌드 성공 확인
2. 구어체/금지단어/접속사 다양성 확인
3. 모든 URL 실제 존재 확인

---

## 8. 레퍼런스

골격: 서론 → H2 섹션들 → FAQ → 출처 → 면책.
코드 구조 참고: `src/app/w/퇴직금-소득세/page.tsx`
**구조와 컴포넌트는 Q1-Q4 답을 기반으로 매번 새로 설계한다. 레퍼런스의 H2를 복사하지 않는다.**

---

## 9. 조립 패턴

### 서론
"[독자 고민]?" → 핵심 답변 1줄 → 동기부여 1줄 → 이 글로 해결된다 1줄

### 섹션 (매 섹션 반복)
텍스트 2~3문단 → SectionBadge → 컴포넌트 1개 → 마무리 1~2문단

### 데이터
파일 상단 const 전체 선언 → JSX에서 props로 주입
