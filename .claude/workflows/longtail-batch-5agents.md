# 롱테일 배치 작성 워크플로우 (5개 서브 에이전트)

> **애드센스 월 1억 전략**: 토큰 최소화 + 정확도 100% + 속도 5배

---

## 🎯 실행 명령어

```
"[키워드 리스트] 5개 에이전트 병렬로 작성해줘"
```

**예시**:
```
"80개 키워드를 longtail-batch-5agents 워크플로우로 작성해줘"
```

---

## ⚙️ 자동 설정

### 서브 에이전트 구성
```yaml
agents: 5개 병렬 실행
distribution: 자동 균등 분배 (80개 → 16개씩)
compression: --uc (30-50% 토큰 절감)
model: sonnet (정확도 우선)
```

### 웹검색 최적화
```yaml
strategy: 스마트 검색
sources:
  - 정부기관만 (법제처, 국세청, 고용노동부 등)
  - 네이버 블로그 제외
caching:
  - 같은 출처 1회만 검색
  - 5개 에이전트 검색 결과 공유
  - 세션 내 캐싱
deduplication: true
```

### 템플릿 기반 생성
```yaml
template: .claude/references/wegive-template-full.md
structure_reuse: true (구조 동일, 내용만 변경)
style: wegive-style (구어체 필수)
token_saving: 40%
```

---

## 📊 비중 전략 (애드센스 수익 최적화)

### 카테고리 분배
```yaml
download_forms: 50%  # 📥 다운로드 양식 (트래픽 + 전면광고)
loans_insurance: 30% # 💰 대출/보험 (고단가 클릭)
tax_finance: 20%     # 📉 세금/금융 (기초 체력)
```

### 우선순위 매트릭스
| 순위 | 카테고리 | 검색량 | RPM | 전략 |
|------|----------|--------|-----|------|
| 🏆 1위 | 다운로드 양식 | 매우 높음 | 3,000원 | 전면광고 |
| 💎 2위 | 대출/보험 | 높음 | 8,000원 | 고단가 클릭 |
| 📚 3위 | 세금/금융 | 중간 | 4,000원 | SEO 체력 |

---

## 🚀 실행 프로세스

### 1단계: 키워드 분류 (자동)
```python
keywords_80 = [키워드 리스트]

# 자동 분류
download = keywords_80[:40]  # 50%
loans = keywords_80[40:64]   # 30%
tax = keywords_80[64:80]     # 20%
```

### 2단계: 5개 에이전트 병렬 실행
```javascript
// 한 번에 5개 Task 호출
Task 1: keywords[0:16]   --uc --template wegive
Task 2: keywords[16:32]  --uc --template wegive
Task 3: keywords[32:48]  --uc --template wegive
Task 4: keywords[48:64]  --uc --template wegive
Task 5: keywords[64:80]  --uc --template wegive
```

### 3단계: 웹검색 최적화 (자동)
```yaml
각 에이전트:
  - 첫 검색 결과 캐싱
  - 같은 출처 재검색 방지
  - 정부기관 우선 검색
  - 2026년 최신 정보 확인
```

### 4단계: 품질 검증 (자동)
```yaml
validation:
  - wegive 스타일 준수
  - keywords 4-5개
  - FAQ 3개 고정
  - 내부링크 3개 이상
  - 출처 URL 실제 존재
```

### 5단계: Git 커밋 & 배포
```bash
git add content/wiki/*.md
git commit -m "feat: 롱테일 80개 배치 작성 (5 agents)"
git push
npm run build
```

---

## 💰 토큰 절감 전략

### 절감 기법 조합
```yaml
compression: --uc (30-50% 절감)
web_search_cache: 정부 사이트 캐싱 (40% 절감)
template_reuse: 구조 재사용 (40% 절감)
batch_context: 배치 작업 (20% 절감)

총 절감률: 약 70%
```

### 비용 비교 (80개 문서)
| 방법 | 시간 | 토큰 | 비용 |
|------|------|------|------|
| 순차 작업 | 6시간 | 100% | $15 |
| 병렬 5개 | 1.5시간 | 100% | $15 |
| **이 워크플로우** | **1.5시간** | **30%** | **$4.5** |

**절감액**: $10.5 (70% 절감)

---

## 📥 다운로드 양식 특화 전략

### 양식 문서 구조
```yaml
title: "[양식명] 양식 무료 다운로드 작성법 2026"
description: "[양식명] 양식 무료로 바로 다운받으세요. 작성법까지 3분이면 끝나요."
category: "양식·서식"

sections:
  1. 양식 미리보기 (표 형태)
  2. 무료 다운로드 버튼 (전면광고 트리거)
  3. 작성법 (차별화!)
  4. 주의사항
  5. FAQ

cta:
  - 다운로드 버튼 → 전면광고 (RPM 3,000원)
  - 내부링크 → SEO
```

### 1순위 양식 (트래픽 폭탄)
```
근로계약서 (월 10만+ 검색)
사직서 (월 8만+ 검색)
임대차계약서 (월 20만+ 검색)
위임장 (월 5만+ 검색)
차용증 (월 4만+ 검색)
```

---

## ⚠️ 오차 방지 (100% 정확도)

### 필수 검증 항목
```yaml
web_search: 매번 실행 (캐시 재사용해도 최신 확인)
government_only: 정부기관 출처만
2026_info: 2026년 1월 기준 정보
calculations: 계산식 검증 (예: 10,000 × 0.165 = 1,650)
legal_accuracy: 법령 최신 버전 확인
```

### 자동 검증 스크립트
```python
# scripts/validate-longtail.py 실행
- 숫자 오차 체크
- 세율 정확도 (16.5%, 13.2%)
- 법정 기한 (14일, 3년 등)
- 출처 URL 유효성
```

---

## 🎯 성과 목표 (3월까지)

### 트래픽 목표
```
현재: 일 500 PV
1개월: 일 5,000 PV (10배)
2개월: 일 20,000 PV (40배)
3개월: 일 50,000 PV (100배)

전략: 다운로드 양식 50% 비중
```

### 수익 목표
```
현재: 월 수천원
1개월: 월 10만원
2개월: 월 50만원
3개월: 월 100만원+

RPM: 3,000원 (다운로드 전면광고)
필요 PV: 일 1만 PV → 월 30만 PV
```

---

## 🔄 반복 사용 방법

### 매일 사용 시
```
1. 키워드 리스트 준비 (80개)
2. "longtail-batch-5agents 워크플로우로 작성해줘"
3. 1.5시간 대기
4. 완성 확인 → 빌드 & 배포
```

### 키워드 확장 시
```
- 100개: 5 agents × 20개씩
- 150개: 5 agents × 30개씩 (2시간)
- 300개: 5 agents × 60개씩 (3-4시간)
```

---

## 🎨 패턴 다양성 전략 (AI 티 방지!)

> **핵심 원칙**: 같은 의미도 매 글마다 다르게 표현 → 자연스러운 변화

### 문제: AI 같은 반복 패턴
```
❌ 80개 글이 전부 "예를 들어볼게요"로 시작
❌ 모든 섹션이 "쉽게 말하면"으로 시작
❌ 매번 "오늘은 ~에 대해 알아볼게요"
```

### 해결: 5개 에이전트별 패턴 사전 할당

#### Agent 1 (키워드 1-16): 패턴 세트 A
```yaml
예시_전환구: "예를 들어볼게요" (1회만)
설명_시작: "쉽게 말하면"
상황_제시: "이런 경우가 있어요"
```

#### Agent 2 (키워드 17-32): 패턴 세트 B
```yaml
예시_전환구: "예를 들면" (1회만)
설명_시작: "다시 말해"
상황_제시: "상황을 보면"
```

#### Agent 3 (키워드 33-48): 패턴 세트 C
```yaml
예시_전환구: "한번 볼까요?" (1회만)
설명_시작: "풀어서 말하면"
상황_제시: "이런 경우예요"
```

#### Agent 4 (키워드 49-64): 패턴 세트 D
```yaml
예시_전환구: 전환구 없이 바로 예시 시작
설명_시작: 바로 설명 시작 (전환구 생략)
상황_제시: "실제로는"
```

#### Agent 5 (키워드 65-80): 패턴 세트 E
```yaml
예시_전환구: "구체적으로 보면" (1회만)
설명_시작: "간단히 말해"
상황_제시: "실제 상황으로"
```

### 패턴 변주 리스트 (참고용)

**"예를 들어볼게요" 변주** (0-1회 사용):
- 예를 들어볼게요
- 예를 들면
- 한번 볼까요?
- 구체적으로 보면
- 실제 상황으로
- (전환구 없이 바로 예시)

**"쉽게 말하면" 변주**:
- 쉽게 말하면
- 다시 말해
- 풀어서 말하면
- 간단히 말해
- (바로 설명 시작)

**"이런 경우" 변주**:
- 이런 경우가 있어요
- 상황을 보면
- 이런 경우예요
- 실제로는
- 예시로

---

## ⚠️ 위기브 스타일 핵심 규칙 (엄수!)

> **wegive-template-full.md 100% 준수 필수**

### 서론 작성 규칙
```yaml
길이: 공백 포함 160자 이내 (엄격!)
시작_금지: "오늘은", "오늘", "지금부터"
구성: 독자 공감 → 핵심 답변 (4-5문장)
패턴_다양성: 매 글마다 다른 전개 방식
```

**❌ 금지 예시**:
```markdown
"오늘은 계약갱신청구권에 대해 알아볼게요!" (165자)
```

**✅ 올바른 예시**:
```markdown
"전세 2년 살았는데 더 살고 싶어요. 집주인한테 어떻게 말해야 하죠?"

계약갱신청구권을 쓰면 돼요. 집주인이 거절 못하고, 월세/전세금 5% 제한돼요. 근데 타이밍 놓치면 권리 자체가 사라져요. (152자)
```

### A씨 B씨 예시 규칙 (선택적 사용!)

**⚠️ 필수 아님 - 복잡한 계산 예시만 선택적 사용!**

```yaml
사용_기준: 복잡한 계산/비교만 (퇴직금 지연이자, 청약 가점, 세액공제 차이)
주제당: 0-1회 (과도한 사용 금지)
간단한_설명: A씨/B씨 없이 직접 설명
최대_인원: 2명 (A씨, B씨만)
```

**❌ 금지**:
```markdown
A씨는 전입신고를 했어요. 그래서 대항력을 받았어요.
C씨, D씨, E씨... (3명 이상)
```

**✅ 올바름** (복잡한 계산만):
```markdown
월급 300만원, 재직 2년이면 퇴직금은 600만원이에요.
(A씨 없이 직접 설명)

예를 들어볼게요. A씨는 1,000만원을 30일 지연해서 164,384원 추가로 받았어요.
(복잡한 계산 예시 - 허용)
```

### 패턴 사용 횟수 제한
```yaml
"예를 들어볼게요": 0-1회 (절대 2회 이상 금지!)
"쉽게 말하면": 제한 없음 (하지만 다양하게 변주)
"A씨 B씨": 0-1회 ("예를 들어볼게요"와 함께)
```

### 독자 니즈 중심 작성 (신규 지침!)
```yaml
목표: "이 사이트에서 다 해결됐으니 다른 곳 갈 필요 없음"
대상: 20~80대 노인까지 이해 가능
완성도: 궁금증 및 문제해결 100%
```

**소제목 내용 작성**:
- **핵심만 간결하게** (너무 길지도, 너무 짧지도 않게)
- **20~80대 누구나 이해 가능한 설명**
- **전문용어 사용 시 바로 풀어서 설명**
- **"다른 사이트 찾아봐야겠다" 안 들게 충분한 설명**
- **실제 상황 예시 포함으로 이해 도움**

---

## 🎨 위기브 최신 패턴 (2026.01.20 추가)

### 🔥 테이블 + 텍스트 조합 (필수 패턴!)

**⚠️ 테이블만 달랑 있으면 안 됨! 앞뒤 텍스트 필수!**

```yaml
패턴_구조: [도입 텍스트] → 테이블 → [요약/팁 텍스트]
테이블_전: 무엇을 비교하는지 설명 (1-2문장)
테이블_후: 요약 + 꿀팁 (귀찮은 사람을 위한 팁!)
```

**실제 예시**:
```markdown
청약 신청하고자 하는 지역을 확인한 후, 주택 면적에 따른 1순위 청약 예치금을 확인해 주세요.

| 면적 구분 | 서울/부산 | 기타 광역시 | 기타 시/군 |
|----------|----------|------------|-----------|
| 85㎡ 이하 | 300만 원 | 250만 원 | 200만 원 |
| 102㎡ 이하 | 600만 원 | 400만 원 | 300만 원 |

**청약 예치금을 신경 쓰기 귀찮으신 분들은 청약 통장에 1,500만 원 이상 넣어두시면 모든 면적에 신청할 수 있어요.**
```

### 🎯 콜아웃 박스 (강조 문구)

**중요 내용은 인용문으로 강조:**
```markdown
> "주택 종류에 따라 청약 1순위 조건은 다릅니다."

> "1순위 요건이 충족되면, 가점제를 활용해 당첨자를 뽑습니다."
```

**사용 케이스**:
- 핵심 결론/요약
- 주의사항 강조
- 섹션 전환 시 중요 포인트

### 📋 번호 리스트 (조건 나열)

**복잡한 조건은 ①②③ 형식:**
```markdown
**국민주택 청약 1순위 조건**

① 수도권: 주택청약저축 1년 이상 가입, 12회 이상 납부
② 수도권 외: 주택청약저축 6개월 이상 가입, 6회 이상 납부
③ 투기과열지역:
   - 2년 이상 주택청약저축 가입, 24회 이상 납부
   - 해당 지역에 2년 이상 거주
```

**사용 케이스**:
- 자격 조건 (3개 이상)
- 신청 서류 목록
- 단계별 절차 (테이블 대신)

### 🔥 비교 테이블 + 링크 (CTR 30-50%!)

**뱅크샐러드 스타일 - 항목별로 링크 연결!**
```markdown
| 은행 | 기본금리 | 우대금리 | 신청 |
|------|---------|---------|------|
| [NH농협은행](https://banking.nonghyup.com) | 4.50% | 최대 1.00%p | [신청하기](링크) |
| [신한은행](https://www.shinhan.com) | 4.50% | 최대 1.00%p | [신청하기](링크) |
```

**왜 효과적인가**:
- 독자: "여기서 다 비교하고 바로 신청할 수 있네!"
- CTR: 10% → 30-50% (전면광고 트리거!)
- 위기브 철학: "이 사이트에서 다 해결" 완벽 구현

**적용 케이스**:
- K패스 카드: 농협/국민/신한 카드별 비교
- 청년도약계좌: 은행별 금리/우대조건 비교
- 민생지원금: 지자체별 금액/신청기간 비교

---

## 🔍 검증 워크플로우 (자동)

### 1단계: 패턴 횟수 체크
```bash
# "예를 들어볼게요" 횟수 확인 (0-1회만 허용)
grep "예를 들어볼게요" content/wiki/*.md | wc -l

# 각 파일별 횟수 (2회 이상 = 위반!)
grep -c "예를 들어볼게요" content/wiki/파일명.md
```

### 2단계: 서론 길이 체크
```python
# Python으로 서론 추출 및 길이 체크
import re

def check_intro_length(file_path):
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # frontmatter 이후, 첫 H2 이전 내용 추출
    intro = re.search(r'---\n.*?---\n\n(.*?)\n##', content, re.DOTALL)
    if intro:
        intro_text = intro.group(1).strip()
        length = len(intro_text)  # 공백 포함
        if length > 160:
            print(f"❌ {file_path}: {length}자 (160자 초과!)")
        else:
            print(f"✅ {file_path}: {length}자")
```

### 3단계: A씨 B씨 C씨 체크
```bash
# C씨 사용 검사 (있으면 위반!)
grep "C씨" content/wiki/*.md

# 결과: 0건이어야 함
```

### 4단계: "오늘은" 금지 패턴 체크
```bash
# 서론에 "오늘은" 사용 검사
grep "오늘은\|오늘\|지금부터" content/wiki/*.md

# 결과: 0건이어야 함
```

### 5단계: 종합 검증 스크립트
```bash
# scripts/validate-patterns.sh
#!/bin/bash

echo "=== 패턴 검증 시작 ==="

# 1. "예를 들어볼게요" 2회 이상 파일 찾기
echo "1. 패턴 중복 체크..."
for file in content/wiki/*.md; do
  count=$(grep -c "예를 들어볼게요" "$file" 2>/dev/null || echo 0)
  if [ "$count" -ge 2 ]; then
    echo "❌ $file: $count회 (1회 초과!)"
  fi
done

# 2. C씨 사용 체크
echo "2. C씨 사용 체크..."
grep -l "C씨" content/wiki/*.md 2>/dev/null && echo "❌ C씨 발견!" || echo "✅ C씨 없음"

# 3. "오늘은" 체크
echo "3. 금지 패턴 체크..."
grep -l "오늘은\|오늘\|지금부터" content/wiki/*.md 2>/dev/null && echo "❌ 금지 패턴 발견!" || echo "✅ 금지 패턴 없음"

echo "=== 검증 완료 ==="
```

---

## 📊 현실적 기대치

### 목표 준수율
```yaml
패턴_다양성: 80-90% (5개 에이전트 사전 할당으로)
수동_보정: 10-20% (자연스러운 흐름 위해 필요)
자동_검증: 100% (스크립트로 위반 체크)
```

### 왜 100% 완벽은 어려운가?
1. **에이전트는 독립적으로 작동** → 서로 패턴 모름
2. **자연스러운 흐름 > 완벽한 패턴 분배** → 때로는 같은 표현이 더 자연스러울 수도
3. **16개씩 작성하다 보면** → 무의식적 반복 가능

### 해결 방법
1. **사전 할당으로 80-90% 준수** (패턴 세트 A~E)
2. **검증 스크립트로 위반 자동 감지** (Grep, Python)
3. **10-20%는 수동 보정** (자연스러운 흐름 유지하며)
4. **우선순위**: 자연스러움 > 완벽한 다양성

### 실제 작업 플로우
```yaml
1. 5개 에이전트 병렬 실행 (패턴 세트 A~E 할당)
2. 검증 스크립트 실행 (자동 위반 체크)
3. 위반 파일만 수동 보정 (10-20%)
4. 최종 확인 후 배포

예상_시간: 1.5시간 (80개) + 20분 (보정) = 1시간 50분
품질: 80-90% 자동 준수 + 10-20% 수동 완성도 = 100%
```

---

## 📝 체크리스트 (자동 적용)

### 기본 설정
- [x] 5개 서브 에이전트 병렬
- [x] --uc 압축 모드
- [x] 웹검색 캐싱
- [x] 정부 사이트만 검색
- [x] 2026년 최신 정보

### 패턴 다양성 (신규!)
- [x] Agent 1-5별 패턴 세트 A~E 할당
- [x] "예를 들어볼게요" 0-1회 (각 파일)
- [x] A씨 B씨 2명만 (C씨 금지)
- [x] 서론 160자 이내 (공백 포함)
- [x] "오늘은" 금지 패턴 체크
- [x] 검증 스크립트 실행 (자동)

### 독자 니즈 중심 (신규!)
- [x] 소제목 내용: 핵심만 간결하게
- [x] 20~80대 누구나 이해 가능
- [x] "다른 곳 갈 필요 없음" 100% 해결

### 위기브 최신 패턴 (2026.01.20 추가!)
- [x] 테이블 + 텍스트 조합 (앞뒤 텍스트 필수)
- [x] 콜아웃 박스 활용 (중요 내용 강조)
- [x] 번호 리스트 (①②③ 형식)
- [x] 비교 테이블 + 링크 (CTR 최적화)
- [x] A씨/B씨 선택적 사용 (복잡한 계산만)

### 위기브 스타일
- [x] wegive 스타일 (구어체)
- [x] keywords 4-5개
- [x] FAQ 3개
- [x] 내부링크 3개+
- [x] 출처 URL 검증
- [x] 계산식 정확도

### 자동화
- [x] Git 커밋 자동
- [x] 패턴 검증 자동

---

**이 파일만 있으면 매일 사용 가능!**
"longtail-batch-5agents 워크플로우로 작성해줘" 한 마디면 끝!
