# 머니위키 (jjyu.co.kr) 리빌드 규칙서

> 이 파일은 머니위키 전면 리빌드의 마스터 규칙서입니다.
> pharm.jjyu.co.kr 구조를 경제/금융에 맞게 적용합니다.

## 프로젝트 개요

- **사이트**: jjyu.co.kr (경제·금융 정보 사이트, "머니위키")
- **스택**: Next.js 16 + Tailwind CSS + shadcn/ui
- **배포**: Vercel (main 브랜치 자동 배포)
- **아키텍처**: Hub/Spoke (허브 = 카테고리 가이드, 스포크 = 개별 키워드 글)
- **디자인**: 금융연합회 포털 스타일 (남색 #1B3A5C 기반, 카드 그리드)
- **작성자**: "머니위키 에디터"

---

## 리빌드 방향

### 핵심 원칙
1. **MD 전면 폐기** → 모든 글을 TypeScript 데이터 객체로 통일
2. **TSX Blog/Spoke/Hub 3중 구조 폐기** → 단일 Spoke 구조
3. **카테고리 정리** → 60개 → 12개로 통합
4. **계산기 49개 + 체커 48개** → 별도 컴포넌트 보존, 글에서 연결

### 데이터 구조 (pharm-jjyu 동일)
```
data/
├── categories.ts          ← 12개 카테고리 정의
└── articles/
    ├── index.ts           ← 통합 export
    ├── 부동산.ts
    ├── 근로.ts
    ├── 세금.ts
    ├── 금융.ts
    ├── 실업급여.ts
    ├── 복지.ts
    ├── 법률.ts
    ├── 퇴직.ts
    ├── 보험.ts
    ├── 생활경제.ts
    └── 교육.ts
```

### 계산기/체커 (기존 유지)
```
src/components/calculators/   ← 49개 그대로 유지
src/components/checkers/      ← 48개 그대로 유지
```
글 데이터에서 `calculator: "SeverancePayCalculator"` 필드로 연결.

---

## 카테고리 (12개)

| slug | 이름 | 아이콘 | 기존 카테고리 병합 | 예상 글 수 |
|------|------|--------|-------------------|-----------|
| 부동산 | 부동산 | 🏠 | 부동산 | ~428 |
| 근로 | 근로·급여 | 💼 | 근로/노동, 근로노동, 근로, 급여, 연차휴가, 휴일, 휴업, 휴직, 노동 | ~428 |
| 세금 | 세금 | 🧾 | 세금, 연말정산, 세금/연말정산 | ~332 |
| 금융 | 금융·투자 | 💰 | 금융, 금융/금전, 금융/투자, 금융/적금, 금융/대출, 경제, 투자, 재테크, 저축 | ~209 |
| 실업급여 | 실업급여 | 📋 | 실업급여 | ~170 |
| 복지 | 복지·지원금 | 🤝 | 복지, 고용/복지, 복지/연금, 정부지원금, 고용, 고용복지 | ~107 |
| 법률 | 법률 | ⚖️ | 법률, 가정법률, 생활법률 | ~86 |
| 퇴직 | 퇴직·연금 | 🏦 | 퇴직금, 퇴직연금, 연금 | ~82 |
| 생활경제 | 생활경제 | 🛒 | 생활, 생활정보, 생활/소비, 교통 | ~40 |
| 보험 | 보험 | 🛡️ | 보험, 보험/금융, 보험/건강, 건강보험 | ~20 |
| 교육 | 교육·장학금 | 📚 | 교육, 교육/장학금 | ~15 |
| 창업 | 창업·사업 | 🚀 | 창업/폐업, 정책, 행정 | ~5 |

---

## URL 구조

```
/                          ← 메인 (카테고리 그리드 + 인기 계산기)
/부동산                     ← Hub 페이지 (카테고리 가이드)
/부동산/양도세-비과세-조건    ← Spoke 페이지 (개별 글)
/calc/퇴직금               ← 계산기 전용 페이지 (선택)
```

> 기존 `/w/{slug}` → 새 `/{category}/{slug}` 로 301 리다이렉트 필수

---

## 글쓰기 규칙

### 1. 타이틀 공식 (롱테일 필수)

```
형식: [메인 롱테일 3단어+] | [연관 롱테일 3단어+]
```

**규칙:**
- 파이프(|) 양쪽 모두 실제 검색어 (3단어 이상)
- 베이스 키워드 양쪽 반복 금지
- 동의어 중복 금지 (조건≈자격≈요건≈대상≈기준 중 1개만)
- 60자 이내
- 금지: "총정리", "완벽정리", "가이드", 콜론(:), 긴대시(—)
- 금지: 파이프 이후 질문형/설명문

```
GOOD: "양도소득세 비과세 1가구 1주택 | 보유기간 거주요건 계산"
GOOD: "실업급여 수급 기간 계산 | 고용보험 가입일수 확인"
GOOD: "퇴직금 중간정산 신청 조건 | 세금 공제 계산 방법"
BAD:  "양도세 비과세 | 2026년 달라지는 점은?"  ← 질문형
BAD:  "양도세 | 계산 방법"                     ← 2단어
```

- **title과 h1은 반드시 동일**

### 2. 문체: 구어체

- "~해요", "~이에요", "~예요" 사용
- "~합니다", "~입니다" 사용 금지
- metaDescription만 예외적으로 문어체 허용
- 자연스럽고 친근한 톤 유지

### 3. 작성자

- 이름: **"머니위키 에디터"**
- 프로필 링크: `/about`

### 4. 날짜

- `datePublished`: 기존 글 원래 날짜 유지 (SEO 보존)
- `dateModified`: 리라이트 날짜
- 신규 작성 시 둘 다 오늘 날짜

---

## 글 구조 (Spoke Article)

### 필수 구성 요소

1. **faq**: 3개 (정확히 3개)
2. **sections**: 5~6개 (카테고리 템플릿별 상이)
3. **calculator**: 연관 계산기 slug (있는 경우만)
4. **checker**: 연관 체커 slug (있는 경우만)

### 카테고리별 섹션 템플릿

#### Template A: 제도·정책형 (실업급여, 복지, 근로 등)
| 순서 | 제목 형식 |
|------|----------|
| 1 | `{키워드} 핵심 요약` |
| 2 | `{키워드} 자격 조건` |
| 3 | `{키워드} 신청 방법` |
| 4 | `{키워드} 금액 계산` |
| 5 | `{키워드} 주의사항` |

#### Template B: 세금·계산형 (세금, 연말정산 등)
| 순서 | 제목 형식 |
|------|----------|
| 1 | `{키워드} 개요` |
| 2 | `{키워드} 계산 방법` |
| 3 | `{키워드} 세율표` |
| 4 | `{키워드} 절세 전략` |
| 5 | `{키워드} 신고 절차` |

#### Template C: 부동산·금융상품형 (부동산, 금융, 대출 등)
| 순서 | 제목 형식 |
|------|----------|
| 1 | `{키워드} 개요` |
| 2 | `{키워드} 자격 요건` |
| 3 | `{키워드} 금리 한도 비교` |
| 4 | `{키워드} 신청 절차` |
| 5 | `{키워드} 주의사항` |

#### Template D: 법률·절차형 (법률, 가정법률 등)
| 순서 | 제목 형식 |
|------|----------|
| 1 | `{키워드} 개요` |
| 2 | `{키워드} 요건` |
| 3 | `{키워드} 절차` |
| 4 | `{키워드} 비용 기간` |
| 5 | `{키워드} 주의사항` |

### 카테고리 → 템플릿 매핑

| 카테고리 | 템플릿 |
|---------|--------|
| 실업급여, 복지, 근로, 교육 | Template A (제도형) |
| 세금 | Template B (세금형) |
| 부동산, 금융, 퇴직, 보험, 창업 | Template C (상품형) |
| 법률 | Template D (법률형) |
| 생활경제 | Template A 또는 C (내용에 따라) |

### 섹션 본문 규칙

- 각 섹션 최소 3문단 (문단 구분: `\n\n`)
- 각 문단 최소 2~3문장
- 전문 용어 사용 후 괄호로 설명
- 숫자/데이터에는 반드시 출처 표기

### FAQ 규칙

- 정확히 3개
- 실제 사용자가 궁금해할 질문
- 답변은 구어체, 2~4문장

---

## 데이터 파일 작성 규칙

### Spoke Article 데이터

```typescript
{
  slug: "양도세-비과세-1가구-1주택",
  categorySlug: "세금",
  datePublished: "2025-06-15",      // 기존 글: 원래 날짜 유지
  dateModified: "2026-03-03",       // 리라이트 날짜
  title: "양도소득세 비과세 1가구 1주택 | 보유기간 거주요건 계산",
  h1: "양도소득세 비과세 1가구 1주택 | 보유기간 거주요건 계산",  // title 동일
  metaDescription: "1가구 1주택 양도소득세 비과세 요건을 정리했습니다. 보유기간 2년, 거주요건, 일시적 2주택 특례까지 한눈에 확인하세요.",
  description: "양도소득세 비과세를 받으려면 보유기간과 거주요건을 충족해야 해요.",
  heroDescription: "양도소득세 비과세는 ... (2~3문장 소개)",
  calculator: "CapitalGainsTaxCalculator",  // 연관 계산기 (없으면 생략)
  checker: null,                             // 연관 체커 (없으면 생략)
  sources: [
    { name: "국세청", url: "https://nts.go.kr/...", date: "2026-03" }
  ],
  faq: [
    { question: "1가구 1주택 비과세 보유기간은?", answer: "..." },
    { question: "일시적 2주택 비과세 기간은?", answer: "..." },
    { question: "거주요건 없이 비과세 가능한가요?", answer: "..." },
  ],
  sections: [
    {
      title: "양도소득세 비과세 개요",
      content: "양도소득세 비과세란...\n\n두 번째 문단...\n\n세 번째 문단..."
    },
    {
      title: "양도소득세 비과세 계산 방법",
      content: "..."
    },
    // ... 5~6개
  ],
}
```

### Hub Article 데이터

```typescript
{
  categorySlug: "세금",
  datePublished: "2025-06-15",
  dateModified: "2026-03-03",
  title: "세금 가이드 | 연말정산 양도세 증여세 절세 방법",
  h1: "세금 가이드 | 연말정산 양도세 증여세 절세 방법",
  metaDescription: "...",
  description: "...",
  heroDescription: "...",
  spokes: [
    { slug: "양도세-비과세-1가구-1주택", title: "...", description: "..." },
    // ...
  ],
}
```

---

## UI/UX 디자인 방향

### 참고: 전국은행연합회 소비자포털 (portal.kfb.or.kr)

#### 색상 테마
- **Primary**: 남색 (#1B3A5C) — 헤더, 히어로, 주요 배경
- **Accent**: 파란 (#3B82F6) — 링크, 버튼, 강조
- **Background**: 흰색 (#FFFFFF) + 연회색 (#F8FAFC) 교차
- **Text**: 검정 (#111827) + 회색 (#6B7280)

#### 메인 페이지 레이아웃
```
┌─────────────────────────────────────────┐
│  머니위키 로고  | 카테고리 네비게이션 (12개) │
├─────────────────────────────────────────┤
│  히어로 (남색 배경)                        │
│  "경제·금융 정보를 한눈에"                  │
│  인기 계산기 6개 아이콘 그리드              │
├─────────────────────────────────────────┤
│  카테고리별 섹션 (카드 그리드 반복)          │
│  세금 | 부동산 | 근로 | 금융 | ...         │
│  각 카테고리 인기글 4~6개 카드              │
├─────────────────────────────────────────┤
│  최신 글 리스트 | 인기 FAQ                  │
├─────────────────────────────────────────┤
│  푸터                                     │
└─────────────────────────────────────────┘
```

#### Spoke 페이지 레이아웃
```
┌─────────────────────────────────────────┐
│  헤더 + 네비게이션                         │
├─────────────────┬───────────────────────┤
│                 │  사이드바 (PC only)     │
│  본문 영역       │  - 같은 카테고리 글 목록 │
│  - 히어로       │  - 계산기 위젯 (있으면)  │
│  - 광고 (hero)  │  - 관련 글 링크         │
│  - 섹션 1~6    │                        │
│  - FAQ         │                        │
│  - 광고 (bottom)│                        │
│  - 작성자 카드   │                        │
├─────────────────┴───────────────────────┤
│  푸터                                     │
└─────────────────────────────────────────┘
```

---

## 계산기 연동 규칙

- 계산기 49개는 `src/components/calculators/`에 기존 그대로 유지
- Spoke 글에서 `calculator` 필드로 연결
- Spoke 페이지 사이드바에 계산기 위젯 렌더링
- 계산기 전용 페이지도 별도 라우트로 유지 가능

```typescript
// 계산기 매핑 (기존 CalculatorLoader.tsx 활용)
const CALCULATOR_MAP: Record<string, string> = {
  "퇴직금-계산기": "SeverancePayCalculator",
  "실업급여-계산기": "UnemploymentBenefitCalculator",
  "연말정산-계산기": "YearEndTaxCalculator",
  // ... 49개
};
```

---

## 광고 설정 (pharm-jjyu 동일)

```typescript
type AdSlotType = "top" | "hero" | "bottom";
// top: 페이지 상단
// hero: 서론 아래 (Display)
// bottom: 페이지 하단 (Multiplex)
```

- Google 자동 광고 + 인터스티셜 활성화
- 모든 버튼/링크: 같은 탭에서 열기 (target="_blank" 사용 금지)

---

## 빌드 & 검증 체크리스트

### 필수 검증
```bash
npm run build          # 빌드 성공 필수
```

### 수동 검증 항목
- [ ] title과 h1이 동일한가
- [ ] 구어체 ("~해요") 사용했는가
- [ ] 섹션 5~6개, FAQ 3개인가
- [ ] 타이틀이 롱테일 공식을 따르는가
- [ ] 기존 URL → 새 URL 리다이렉트가 설정되었는가
- [ ] hub spokes에 새 글이 추가되었는가
- [ ] 출처(sources)가 있는가

---

## 마이그레이션 순서

### Phase 1: 인프라 리빌드
1. 새 카테고리 정의 (`data/categories.ts`)
2. TypeScript 타입 정의 (`lib/types.ts`)
3. Hub/Spoke 페이지 컴포넌트 (pharm-jjyu 이식)
4. 메인 페이지 리디자인 (금융연합회 스타일)
5. 계산기/체커 컴포넌트 이관
6. 광고 슬롯 설정

### Phase 2: 콘텐츠 마이그레이션 (카테고리별)
1. 기존 MD 글 읽기 → TypeScript 데이터 변환
2. 타이틀 롱테일로 재작성
3. 본문 리라이트 (섹션 구조화, 구어체)
4. Hub spokes 엔트리 추가
5. `/w/{old-slug}` → `/{category}/{new-slug}` 리다이렉트 추가

**카테고리별 작업 지시 예시:**
```
"세금 카테고리 332개 마이그레이션해줘"
→ data/articles/세금.ts에 Hub + Spoke 데이터 생성
→ 기존 content/wiki/에서 해당 글 내용 참고
→ 새 타이틀 + 구어체 리라이트
```

### Phase 3: 정리
1. 사용하지 않는 MD 파일 삭제
2. 기존 TSX Blog/Spoke/Hub 삭제
3. 전체 빌드 검증
4. 리다이렉트 전수 확인

---

## 5원칙 (위반 시 재작성)

1. **텍스트가 주인공** — 테이블 2개 이하
2. **구어체 필수** — ~이에요, ~해요 (~습니다/됩니다 절대 금지)
3. **독자 중심** — 20~80세 누구나 이해
4. **섹션당 3문단 이상** — 짧으면 AI 티 남
5. **숫자에는 출처** — 출처 없는 숫자 생성 금지

## 정보 정확성 (자주 틀리는 값)

| 항목 | 정확한 값 |
|------|----------|
| 세액공제 (5,500만 이하) | **16.5%** |
| 세액공제 (5,500만 초과) | **13.2%** |
| 퇴직금 지연이자 | **연 20%** |
| 퇴직금 지급기한 | **14일** |
| 청구권 소멸시효 | **3년** |

---

## 파일 구조 (리빌드 후)

```
wiki-site/
├── app/
│   ├── [category]/[slug]/page.tsx  ← Spoke 페이지
│   ├── [category]/page.tsx         ← Hub 페이지
│   ├── calc/[slug]/page.tsx        ← 계산기 페이지
│   ├── about/page.tsx              ← 작성자 소개
│   ├── layout.tsx                  ← 루트 레이아웃 (남색 헤더)
│   └── page.tsx                    ← 메인 (금융연합회 스타일)
├── components/
│   ├── calculators/                ← 49개 계산기 (기존 유지)
│   ├── checkers/                   ← 48개 체커 (기존 유지)
│   ├── CategorySidebar.tsx         ← PC 사이드바
│   ├── FAQSection.tsx              ← FAQ 아코디언
│   ├── AuthorBio.tsx               ← 작성자 카드
│   ├── ShareButtons.tsx            ← 공유 버튼
│   └── AdSlot.tsx                  ← 광고 슬롯
├── data/
│   ├── categories.ts               ← 12개 카테고리
│   └── articles/                   ← 카테고리별 분리
│       ├── index.ts
│       ├── 부동산.ts
│       ├── 근로.ts
│       ├── 세금.ts
│       └── ... (12개)
├── lib/
│   └── types.ts                    ← TypeScript 인터페이스
└── scripts/
    ├── migrate-md-to-ts.js         ← MD → TS 변환 스크립트
    ├── verify-quality.js           ← 품질 검증
    └── generate-redirects.js       ← 리다이렉트 생성
```

---

## Windows 환경 주의사항

- Turbopack dev 서버 불안정 → `npm run build`로 검증
- `.next` 캐시 프로세스 잠금 주의
- preview 대신 빌드 후 Vercel 배포로 확인
